# Server Configuration
PORT=3000
NODE_ENV=development

# Redis Configuration (for job queue)
REDIS_URL=redis://localhost:6379

# LLM Configuration (OpenAI-compatible endpoint)
LLM_API_URL=https://openrouter.ai/api/v1
LLM_API_KEY=your-api-key-here
LLM_MODEL=openai/gpt-4o-mini

# TTS Provider Configuration
# Options: openai, elevenlabs, edge-tts, kokoro
TTS_PROVIDER=openai

# OpenAI TTS Configuration
OPENAI_API_KEY=your-openai-key-here
OPENAI_TTS_MODEL=tts-1

# ElevenLabs Configuration (alternative)
ELEVENLABS_API_KEY=your-elevenlabs-key-here
ELEVENLABS_MODEL=eleven_multilingual_v2

# Kokoro TTS Configuration (via Modal deployment)
# Deploy the model: cd modal && modal deploy kokoro_tts.py
# Then set the URL provided by Modal:
KOKORO_MODAL_URL=https://your-workspace--kokoro-tts-web-app.modal.run

# Storage paths
UPLOAD_DIR=./data/uploads
OUTPUT_DIR=./data/outputs
TEMP_DIR=./data/temp

# Database
DATABASE_PATH=./data/database.sqlite
